<Window x:Class="GameOfEconomy.SetZeroYear"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GameOfEconomy"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="SetZeroYear" Height="523.828" Width="344.727">
    <Grid>


        <ListView x:Name="InstrList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Path=Name}" Width="200"/>
                        <TextBox Text="{Binding Path=Value}" Width="100"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Button Content="Play" Margin="0,0,10.4,10.4" HorizontalAlignment="Right" Width="120" Height="34" VerticalAlignment="Bottom" Click="Button_Click" />

    </Grid>
</Window>
